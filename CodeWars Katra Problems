# make a function that filters strings from a list.

def filter_list(l):
    for x in l:
        if str in list:
            l.remove(str)
        else:
            continue

    return l

a = 'a'

print(type(a))

print(type([1,2,'a','b']))
print(filter_list([1,'a','b',0,15]))
