# make a function that filters strings from a list.

def filter_list(l):
    FilteredList = []
    for x in l:
        if type(x) != str:
            FilteredList.append(x)
    return FilteredList

a = 'a'

print(type(a))

print(type([1,2,'a','b']))
print(filter_list([1,'a','b',0,15]))
